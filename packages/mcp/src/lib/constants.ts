/**
 * Snapshot date of the static registry (`packages/registry/src/xstocks.json`).
 * This is NOT "now" — it only moves when the registry data itself is
 * refreshed. Bump it in lockstep with a registry update, never on its own.
 */
export const REGISTRY_AS_OF = "2026-09-12";
