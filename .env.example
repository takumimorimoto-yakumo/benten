SOLANA_RPC_URL=
NEXT_PUBLIC_SOLANA_RPC_URL=
NEXT_PUBLIC_MWA_REMOTE_HOST_AUTHORITY=
# Server-only upstream for the read-only JSON-RPC relay at /api/solana-rpc.
# May hold a provider URL with a credential: never prefix it with NEXT_PUBLIC_
# and never commit a value. Empty means https://api.mainnet-beta.solana.com.
SOLANA_RPC_UPSTREAM_URL=
