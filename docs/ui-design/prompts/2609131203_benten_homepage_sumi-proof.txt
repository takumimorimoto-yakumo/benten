Create a high-fidelity desktop web UI mockup for Benten, a public open-source developer tool for Solana xStocks financial facts. This is a UI visual-direction image, shown as a 1440 x 1000 first viewport, no browser chrome. Audience: a hackathon judge or Solana agent developer. Single job: paste an xStocks mint and immediately understand the token, underlying company, and the source trail for verified financial facts.

Visual direction: calm, exact, restrained, quietly Japanese and wabi-sabi without Japanese patterns, paper texture, calligraphy, or cultural decoration. Use a cool mineral-white background (#EEF0EC), crisp near-white sheets, deep sumi-blue text (#14252B), muted indigo (#27485C), patinated green (#2F665A), and one small cinnabar accent (#A84A3A) only as a provenance seal/focus detail. IBM Plex Sans-like UI typography; a clean mono face only for mint and code. Thin rules, 4-8px corner radii, no gradients, no glass, no shadows beyond a barely perceptible lift, no card soup, no stock charts. The memorable element is a horizontal proof rail connecting TOKEN to COMPANY to FILING, expressed through structure rather than decoration.

Composition:

1. Quiet header: wordmark “Benten” at left; compact links “How it works”, “Registry”, “GitHub” at right.
2. Hero is a disciplined 5/7 split. Left: headline “Start with a Solana mint. End at the filing.” Supporting text: “Resolve token identity, underlying company, and source-backed financial facts for agents. Facts only.” A small line says “No wallet required.” Right: a large working lookup panel with visible label “xStocks mint”, a full-width mono input containing “Xsc9qvGR1efVDFGLrVsmkzv3qi45LTBjeUKSPmx9qEh”, primary button “Resolve mint”, secondary text action “Try NVDA”. Clearly visible keyboard focus ring.
3. Directly below, show a horizontal proof rail as three linked sections: “NVIDIA xStock” with symbol “NVDAx” and ticker “NVDA”; then “NVIDIA Corporation” with a text badge “Issuer verified”; then “SEC filing” with fields “10-K”, “Period end —”, “Filed —”, and an “Open filing” link. Add a small cinnabar circular seal beside “Source verified”. No claim that placeholder facts are already verified.
4. Lower two-column zone. Left is a clean financial fact sheet titled “Verified facts”, with three rows: “Revenue —”, “Net income —”, “Total assets —”. Every row includes visible metadata placeholders “USD · period —” and a small “Source verified” text marker. Above rows, show the exact notice: “Illustrative preview — values appear only after source verification.” Make the financial values large and easy to scan, but use em dashes rather than invented numbers.
5. Right is a dark-ink inset panel titled “Use Benten in your agent” with a compact MCP setup snippet using “node packages/mcp/dist/index.js” and a button “Copy MCP setup”. Keep code concise; do not invent testimonials or usage metrics. Beneath it, show three plain state links: “Known mint”, “ASML · No current data”, “Unknown mint”.
6. At the bottom edge of the viewport, a thin coverage strip, not cards: “154 registry entries” | “129 filing-eligible” | “128 snapshot available”. Add a quiet link “Browse the registry”. The full 154-row table is below the fold and not visible.

Do not make wallet connection prominent. Do not show trading, prices, recommendations, forecasts, ROI, portfolio value, tokens as coins, fake customer logos, testimonials, performance claims, or fabricated financial values. Do not use a hero illustration. Avoid ALL CAPS labels, oversized decorative numbers, pills everywhere, rounded SaaS cards, glowing crypto neon, purple gradients, and generic dashboard chrome. The image is a visual direction; precise copy and data will be implemented from the public contract.
